# 自建音乐 NAS App - Stitch 界面设计与功能需求文档

本文档旨在为 Stitch (或其他 UI 生成 AI) 提供明确的应用程序功能列表、播放交互要求以及后端 API 对接指南，用于生成一个现代化的音乐 App 界面。

## 1. 项目概述

这是一个连接到自建 NAS (Node.js + SQLite) 音乐库后台的移动端/桌面端全平台音乐播放前端 App。App 需要具备高颜值的 UI、流畅的交互动画，能够处理多格式本地音乐、显示内嵌/在线歌词并呈现沉浸式播放体验。

## 2. 核心功能点 (Feature List)

### 2.1 音乐库浏览与管理 (Library & Navigation)

* **侧边栏/底部导航**：在不同功能页面间切换（如：音乐库主页、文件夹视图、设置）。
* **层级文件夹穿透**：支持递归浏览音乐库的文件夹结构，点击文件夹进入子目录。
* **歌曲列表展示**：
  * 显示每首歌曲的标题、艺术家、专辑信息。
  * 显示刮削状态（Scrape Status：成功、失败、待处理），可作为右上角徽章。
  * 显示文件扩展名（如 MP3, FLAC）。

### 2.2 音乐播放与控制 (Playback)

* **全局悬浮迷你播放器**：位于屏幕底部，随时显示当前播放的歌曲名、艺术家和播放/暂停按钮。
* **沉浸式全屏播放页**：
  * **动态背景**：基于当前封面颜色的 Aurora 渐变或毛玻璃背景。
  * **黑胶唱片动画**：播放时专辑封面旋转，暂停时停止。
  * **基础控制板**：播放/暂停、上一首、下一首、随机播放、列表循环。
  * **进度控制**：支持拖动的进度条，显示当前时间与总时长。

### 2.3 歌词与元数据 (Lyrics & Metadata)

* **歌词同步滚动**：根据音频的时间戳高亮显示当前歌词，居中对齐，支持平滑滚动（Smooth Scrolling）。
* **封面显示**：加载并显示歌曲内嵌的 Cover 图片资源。

### 2.4 后台管理集成 (Dashboard & Control)

* **状态概览面板**：显示数据库整体情况（总数、成功、失败、未刮削）。
* **触发后台任务**：一键触发 NAS 端的“增量扫描” (Scan) 和 “自动刮削” (Scrape) 任务。

## 3. 播放与 UI 交互规范 (Playback Interactions)

1. **点击歌曲列表项**：
    * 将该歌曲所在的整个列表（当前文件夹）更新为**播放队列 (Playlist)**。
    * 立刻开始播放被点击的歌曲。
    * 自动唤起 / 滑出全屏播放页。
2. **全屏播放页交互**：
    * **下拉或点击关闭按钮**：收起全屏主播放页，恢复到迷你悬浮播放器。
    * **滑动歌词**：用户手动滑动歌词区时，暂停自动滚动几秒钟，方便用户查看歌词上下文。
3. **动画要求**：
    * **页面切换**：需使用淡入淡出 (Fade) 或滑动 (Slide) 顺滑切换。
    * **按钮反馈**：播放、按钮点击时有缩放 (Scale) 的触觉反馈动画。
    * **加载状态**：请求列表或封面时展示骨架屏 (Skeleton) 或脉冲 Loading 图标。

## 4. 后端 API 对接大纲 (API Endpoints)

该 App 将对接以下现有的 RESTful API (Host 为 `http://你的IP:8002`):

* **1. 获取服务器总状态**
  * `GET /api/status`
  * 返回：`{ success: true, dbStatus: { total, pending, success, failed }, musicDir }`
* **2. 获取歌曲列表 (支持按文件夹)**
  * `GET /api/tracks?folder=路径`
  * 返回：`{ success: true, data: { folders: [], tracks: [] } }`
  * *注：`tracks` 数组包对象含 `id`, `filepath`, `title`, `artist`, `album`, `extension` 等字段。*
* **3. 获取并播放音频流**
  * `[Audio src] = /api/tracks/:id/stream`
  * 支持 HTTP 206 断点续传和 Range 请求，支持 `.mp3` 与 `.flac`。
* **4. 获取封面图片**
  * `[Image src] = /api/tracks/:id/cover`
  * 返回二进制图片数据流。
* **5. 获取歌词**
  * `GET /api/tracks/:id/lyrics`
  * 返回：`{ success: true, lyrics: "[00:00.00]文本..." }`
* **6. 触发后台扫描/刮削任务**
  * `POST /api/trigger-scan`
  * `POST /api/trigger-scrape`

## 5. Stitch 生成提示 (Prompt 给 AI 的建议)
>
> "请使用 Flutter 构建一个现代化的跨平台暗黑风格音乐播放器应用。应用具备侧边栏（或底部导航栏）和主浏览区域，使用拟物化或 Glassmorphism（结合 BackdropFilter）样式。底部要求有一个随页面全局驻留的 Music Player Bar 悬浮组件。在点击歌曲时，使用 Hero 动画弹出一个覆盖全屏的沉浸式播放窗口：上方是大型悬浮并缓慢旋转的专辑封面，下方需要有一个能够按照时间线高亮平滑滚动的 LRC 歌词展示区（如适用横屏，可左右分屏显示）。所有的操作按钮请使用 Material Design 规范，并附带水波纹特效及激活时的缩放动画反馈。请结合上述 API 规范使用 dummy data 进行模拟并渲染界面，推荐使用 Riverpod 或 Provider 进行播放器全局状态维护。"
