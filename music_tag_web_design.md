# 自动音乐标签刮削器 (Music Tag Auto-Scraper) - 系统设计与进度指南

本项目是一个专为个人 NAS 用户设计的本地音乐元数据管理与**全自动刮削**工具。它集成了音频标签读取、在线数据检索、批量重命名、自动分级整理、去重以及美观的 Web 管理界面。

## 1. 核心需求与已实现功能

目前系统已具备以下核心能力：

- **📦 多格式支持**：深度支持 **MP3** (ID3v2) 与 **FLAC** (Vorbis Comment) 格式的标签读写，包括内嵌封面与歌词。
- **🤖 全自动刮削引擎**：整合 iTunes、NetEase Cloud Music、QQ Music 与 LRCLIB API，自动补全缺失的标题、艺术家、专辑及同步歌词。
- **🔍 沉浸式手动校准**：全新的双栏元数据编辑面板，支持在检索源中进行精准搜索并一键应用。具备自动封面图代理解析功能（突破 CDN 防盗链限制）。
- **📂 自动化文件收纳**：支持根据刮削信息（歌手/专辑）自动整理并创建存放的层级目录，彻底解决音乐收藏乱置问题。
- **🧹 一键格式化重命名**：提供“歌手 - 标题.xxx”格式的一键全局物理文件重命名方案。
- **🚨 跨界智能去重引擎**：搭载针对曲库深度扫描的“查杀重复歌曲”中枢。只需单击即可聚合全盘重名文件，自动辨认并选拔最大/最优质版本，对次品提供物理级抹除。
- **🖥️ 现代化 UI 与反馈**：采用左右侧边栏沉浸式导航，集成了全局无缝 Toast 消息通知、LRC 歌词标记探测，以及炫酷的暗黑风危险动作授权模态框。
- **🎵 沉浸式音乐播放器**：
  - **全屏模式**：黑胶唱片旋转动画、动态环境光背景及巨幕歌剧式视觉体验。
  - **智能歌词**：自动剔除时间轴标签，实现歌词精准对齐与平滑居中滚动。
  - **播放队列**：支持文件夹一键连播、自动播放下一首、手动切歌及实时队列管理。
- **� 高级音乐下载器**：支持网易云、QQ 音乐链接解析。具备**音频质量探针**，支持 320k、无损 (FLAC)、Hi-Res 乃至母带 (Master) 音质选择，并内置全网平替搜索逻辑。
- **�🚀 零死角增量扫描**：采用了原生 UUID 作为唯一主键，即使面对曲库的反复移位和重覆盖，也能避免数据库崩溃碰撞漏洞，随时热重载曲目。
- **⚙️ 全局配置中心**：统一设置管理（settings.json），支持下载偏好与系统行为自定义。

---

## 2. 系统技术架构

项目基于全栈 TypeScript 开发，确保高性能与类型安全：

### 后端 (Backend: Node.js + Express)

- **数据库**: 使用轻量级 **SQLite** (`better-sqlite3`) 存储文件状态与元数据缓存，确保扫描性能。
- **音频引擎**:
  - `node-id3`: 负责 MP3 格式的最佳兼容性读写。
  - `metaflac-js`: 专业处理 FLAC 无损压缩。
- **自动化任务**: 集成 `node-cron` 实现每日定时增量扫描与全自动刮削。
- **文本处理**: 集成 `opencc-js` 自动将繁体中文歌词/标签转换为**简体中文**，保持曲库风格统一。
- **基础套件**: 搭载自行研发的媒体封面反向代理路由引擎，穿透全网 API 屏蔽防护。

### 前端 (Frontend: Vue 3 + Vite)

- **全响应式布局**: 基于 **Tailwind CSS**，专为宽屏设计的 `1400px` 级暗黑沉浸式体验。
- **交互体验**:
  - 全局抽屉式/模态框界面分离体系。
  - 带动画的徽章标识（如扫描状态、LRC 内嵌指示器）。
  - 不使用任何突兀的浏览器原生拦截器（告别 window.prompt / confirm），全量渲染内置气闸。
- **国际化**: 预置支持国际化能力。

---

## 3. 快速开始 (Github 代码仓库)

代码托管于：[Stonelalala/music-tag](https://github.com/Stonelalala/music-tag)

### 开发环境启动

```powershell
# 在根目录运行（包含并行 Vite 与 Nodemon 引擎）
npm run dev
```

### 环境配置项 (.env)

- `MUSIC_DIR`: 音乐存放库路径 (基础驱动点)。
- `PORT`: 后端 API 服务端口（默认 8002）。
- `DATA_DIR`: SQLite 数据库与用户配置集散地。
- `CRON_SCHEDULE`: 自动化任务调度表达式（默认每日凌晨 2 点 `0 2 * * *`）。

---

## 4. 进度表与后续规划 (Roadmap)

- [x] **Phase 1: 核心引擎开发** (已完成)
  - [x] SQLite 状态追踪与 UUID 文件身份系统（根除路径碰撞漏洞）。
  - [x] 复合式刮削逻辑（iTunes, 网易云音乐, QQ音乐联动互补）。
  - [x] 重命名与物理移动文件夹收纳模块。
- [x] **Phase 2: 界面 2.0 进化** (已完成)
  - [x] 左右分栏桌面级布局与媒体库树状穿透。
  - [x] 增强型元数据搜索对比对话框与内置反盗链封面呈现。
  - [x] 基于 Tailwind 完全重构的确认面板与危险动作软态框体系。
- [x] **Phase 3: 清洗与维护中控** (已完成)
  - [x] 前端列表 LRC 内嵌歌词侦测标签。
  - [x] 开发并实装“查杀重复歌曲”一键鉴定与物理销毁器。
- [x] **Phase 4: 播放与获取增强** (已完成)
  - [x] 沉浸式全屏播放器与黑胶动画系统。
  - [x] 歌词时间轴清洗渲染与自动滚动引擎。
  - [x] 播放队列管理与自动连播逻辑。
  - [x] 集成音频探针与多级音质（无损/母带）下载中枢。
- [x] **Phase 5: 自动化与繁简转换** (已完成)
  - [x] 集成 `node-cron` 实现无人值守自动扫描与刮削。
  - [x] 集成 `opencc-js` 实现歌词与标签的自动繁转简。
- [ ] **Phase 6: Docker 工业级部署** (进行中)
  - [ ] 编写多阶段构建的 `Dockerfile`。
  - [ ] 实现 PUID/PGID 权限自动化处理，完美适配群晖/unRAID。
- [ ] **Phase 7: 展望区**
  - [ ] 考虑扩展对 APE 或网易云专有加密格式 NCM 的读取/解密层。

*本设计说明与进度白板遵循 KISS 第一性原则构建，保持精简与硬核体验。*
