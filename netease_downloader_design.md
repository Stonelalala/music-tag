# 网易云音乐下载功能 (Netease Downloader) - 设计方案

## 1. 核心需求与场景
作为 `music-tag` 系统的自然延展，允许用户直接通过输入**网易云音乐的单曲或歌单分享链接（或 ID）**，在系统中实现云端到本地 NAS 的全自动解析、下载、标签写入及整理收纳。

- **支持解析格式**：
  - 单曲链接：`https://music.163.com/#/song?id=123456`
  - 歌单链接：`https://music.163.com/#/playlist?id=123456`
- **一站式流程**：下载音频 -> 获取/写入 ID3 标签（包括高清封面与滚动歌词） -> 自动触发重命名与按艺人/专辑收纳 -> 入库。

## 2. 技术架构与选型

### 2.1 核心数据源 (NeteaseCloudMusicApi)
建议集成开源的 [NeteaseCloudMusicApi](https://github.com/Binaryify/NeteaseCloudMusicApi) 或在后端实现其核心接口封装，主要依赖以下接口：
- `/song/detail`：获取曲目标题、歌手、专辑等元数据。
- `/song/url/v1`：获取歌曲真实的高音质（或无损）播放/下载 URL。
- `/playlist/detail`：获取歌单元数据及其包含的所有曲目 ID 列表。
- `/lyric`：获取逐行同步歌词。

### 2.2 后端处理流 (Backend Stream)
1. **任务分发**：根据传入的是歌曲还是歌单，拆解为多个独立下载任务到内存队列。
2. **异步下载**：原生 Node.js 流式下载 (`axios` responseType: 'stream')。
3. **标签刮削复用**：下载完成后复用现有的刮削方法（已有的 `node-id3` 等能力），将网易云的元数据直接写入刚刚下载的文件中。
4. **收纳与扫库**：复用项目现存的物理重命名与层级创建机制，直接转移到 `MUSIC_DIR` 中。

### 2.3 异常边界与已知限制
- **VIP 加密曲目**：网易云对部分 VIP 曲目以及试听曲目会返回空 URL，或者下载后为受保护的格式（如 `.ncm`）。此第一版设计**仅针对能够获取直链的普通 MP3/FLAC 进行下载**。VIP 阻断情况需要在前端做明确反馈。
- **并发控制**：歌单通常包含多达上百首歌，下载时须配置队列池（如 `p-limit` 限制并发在 3-5 左右），避免被 API 接口风暴熔断屏蔽。

## 3. 前端交互设计 (UI/UX)
为了保持沉浸式与极简感：
1. **全局输入入口**：在侧边栏或顶栏增加「☁️ 云端导入」或「网易云下载」按钮。
2. **抽屉式面板**：点击后从右侧滑出解析抽屉，包含：
   - 一个大号的文本输入框：支持粘贴链接。
   - 一个解析按钮，解析后直接在一张列表或表格中列出即将会下载的歌曲名称、歌手、清晰度状态。
3. **任务进度中心**：确认下载后，抽屉底部或侧边展示实时下载进度条事件徽章。这依赖于后端利用 WebSocket (Socket.io) 或是 SSE 推送当前下载状态（如：`正在下载: 张学友 - 吻别 (65%)`）。

## 4. 实施阶段拆分 (Task Breakdown)

### Phase 1: 后端解析与下载基建
- [ ] 引入 `NeteaseCloudMusicApi` 核心调用能力或单独封装网易云解析网关模块。
- [ ] 实现针对单曲和歌单的解析接口 (`GET /api/netease/parse`)，返回结构化的曲目列表信息。
- [ ] 实现核心下载 Pipeline：下载流式储存至缓存目录 -> 复用旧有函数写 ID3/封面 -> 移动至最终目录。

### Phase 2: SSE/WebSocket 前后端进度透传
- [ ] 部署 SSE (Server-Sent Events) 或改进现有通知机制，确保长时间下载的异步进度能反馈给前端。
- [ ] 后端实现对单个或批量下载任务的队列限流器。

### Phase 3: Vue3 抽屉式交互页面
- [ ] 基于 TailwindCSS 构建“☁️ 云端导入”的右侧抽屉式界面。
- [ ] 链接解析与预览状态展示（歌单卡片与曲目表）。
- [ ] 完成交互对接，对接处理下载失败或 VIP 版权限制的高亮 Toast 提示。

---
*遵循 KISS 原则：我们先实现基于免登录的基础链获取通道，若后续面临严重的防盗链或非 VIP 音质锁死，再考虑引入网易云账号 Cookie 扫码登录录入机制。*
